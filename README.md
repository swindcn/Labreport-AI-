# New Project

React + TypeScript + Tailwind 的前端工作区骨架。

## 当前状态

- 已创建基础目录结构和最小页面。
- 已预留可复用组件目录：`src/components`、`src/sections`、`src/lib`。
- 当前环境未安装 `Node.js` / `npm`，因此还不能在本机执行依赖安装和本地预览。

## 目录

```text
src/
  components/
  sections/
  App.tsx
  main.tsx
  index.css
```

## 后续

1. 安装 Node.js 20+。
2. 执行 `npm install`。
3. 执行 `npm run dev`。
4. 提供 Figma 链接或设计截图，继续实现真实页面。
